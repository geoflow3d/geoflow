name: CI Windows

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - name: Install glfw
      run: |
        mkdir deps
        cd deps
        git clone https://github.com/glfw/glfw.git
        cd glfw
        mkdir build && cd build
        cmake ..
        cmake --build . --target install
    - name: Install glm
      run: |
        cd deps
        git clone https://github.com/g-truc/glm.git
        cd glm
        mkdir build && cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release -DGLM_TEST_ENABLE=OFF
        cmake --build . --target install
    - uses: actions/checkout@v1
    - name: Build
      run: |
        mkdir build && cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release -DGF_USE_EXTERNAL_JSON=OFF
        cmake --build .
